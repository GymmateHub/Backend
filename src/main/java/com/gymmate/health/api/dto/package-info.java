/**
 * Public DTO surface for the health module (exercise/workout/goal/metric responses).
 * No external caller today, but declared as the intentional public surface per the
 * target functional package hierarchy.
 */
@org.springframework.modulith.NamedInterface("api.dto")
package com.gymmate.health.api.dto;
