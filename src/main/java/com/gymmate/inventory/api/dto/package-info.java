/**
 * Public DTO surface for the inventory module (equipment/inventory/maintenance/supplier
 * REST request/response types). No external caller today, but declared as the
 * intentional public surface per the target functional package hierarchy.
 */
@org.springframework.modulith.NamedInterface("api.dto")
package com.gymmate.inventory.api.dto;
