/**
 * Public DTO surface for the admin module (platform-admin/superadmin read views). No
 * external caller today, but declared as the intentional public surface per the
 * target functional package hierarchy.
 */
@org.springframework.modulith.NamedInterface("api.dto")
package com.gymmate.admin.api.dto;
