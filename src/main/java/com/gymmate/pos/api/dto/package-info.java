/**
 * Public DTO surface for the pos module (sale/cash-drawer REST request/response
 * types). No external caller today, but declared as the intentional public surface
 * per the target functional package hierarchy.
 */
@org.springframework.modulith.NamedInterface("api.dto")
package com.gymmate.pos.api.dto;
